# BhashaSetu MVP Environment Configuration

# Set to "true" to skip loading heavy ML models and use deterministic fast outputs (essential for quick demo fallback)
MOCK_MODE=true

# Optional: Set this to use an LLM for summarization/translation (not strictly required)
OPENAI_API_KEY=
